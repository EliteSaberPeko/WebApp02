@model BaseInsertViewModel<PublishingHouse>

<div class="card border-info">
    <div class="card-header border-dark">Список издательств</div>
    <ul class="list-group list-group-flush">
        @foreach(var item in Model.ListItems)
        {
            <li class="list-group-item">
                <div class="d-flex">
                    <div class="me-auto">@item.Name</div>
                    <form method="get" asp-action="PublishingHouse" asp-controller="Update" asp-route-id=@item.Id>
                        <button class="btn btn-outline-success" type="submit">Редактировать</button>
                    </form>
                    <form method="post" asp-action="PublishingHouse" asp-controller="Delete" asp-route-id=@item.Id>
                        <button class="btn btn-outline-danger ms-1" type="submit">Удалить</button>
                    </form>
                </div>
            </li>
        }
    </ul>
    <page-link page-model="Model.Page" page-action="PublishingHousePage"></page-link>
</div>